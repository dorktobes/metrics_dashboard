import React, { Component } from 'react';
import PropTypes from 'prop-types';

class App extends Component {
  constuctor(props) {
    super(props);
  }
  render() {
    return (
      <div>
        App
      </div>
    );
  }
}

App.propTypes = {
  
};

export default App;
